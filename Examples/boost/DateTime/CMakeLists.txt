
option( BUILD_BOOST_DATE_TIME_EXAMPLES 		"Build the boost DateTime examples." ON)

if(BUILD_BOOST_DATE_TIME_EXAMPLES)
	SUBDIRLIST(SUBDIRS ${CMAKE_CURRENT_SOURCE_DIR})

	FOREACH(subdir ${SUBDIRS})
		ADD_SUBDIRECTORY(${subdir})
	ENDFOREACH()
endif()