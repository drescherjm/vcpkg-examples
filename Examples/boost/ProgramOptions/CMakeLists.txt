
option( BUILD_BOOST_PROGRAM_OPTIONS_EXAMPLES 		"Build the boost ProgramOptions examples." ON)

if(BUILD_BOOST_PROGRAM_OPTIONS_EXAMPLES)
	SUBDIRLIST(SUBDIRS ${CMAKE_CURRENT_SOURCE_DIR})

	FOREACH(subdir ${SUBDIRS})
		ADD_SUBDIRECTORY(${subdir})
	ENDFOREACH()
endif()