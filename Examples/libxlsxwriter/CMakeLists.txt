option( BUILD_XLSXWRITER_EXAMPLES 		"Build the xlsxwriter examples." ON)

if(BUILD_XLSXWRITER_EXAMPLES)
	SUBDIRLIST(SUBDIRS ${CMAKE_CURRENT_SOURCE_DIR})
	FOREACH(subdir ${SUBDIRS})
		ADD_SUBDIRECTORY(${subdir})
	ENDFOREACH()
endif()