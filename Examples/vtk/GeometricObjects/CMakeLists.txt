
option( BUILD_VTK_GEOMETRIC_OBJECT_EXAMPLES 		"Build the vtk geometric object examples." ON)

if(BUILD_VTK_GEOMETRIC_OBJECT_EXAMPLES)
	SUBDIRLIST(SUBDIRS ${CMAKE_CURRENT_SOURCE_DIR})

	FOREACH(subdir ${SUBDIRS})
		ADD_SUBDIRECTORY(${subdir})
	ENDFOREACH()
endif()