
option( BUILD_VTK_IMAGES_EXAMPLES 		"Build the vtk images examples." ON)

if(BUILD_VTK_IMAGES_EXAMPLES)
	SUBDIRLIST(SUBDIRS ${CMAKE_CURRENT_SOURCE_DIR})

	FOREACH(subdir ${SUBDIRS})
		ADD_SUBDIRECTORY(${subdir})
	ENDFOREACH()
endif()